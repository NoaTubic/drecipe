import 'package:flutter/material.dart';

class ExploreRecipesScreen extends StatelessWidget {
  const ExploreRecipesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
